/*import { render, screen } from '@testing-library/react';
//components
import Input from './Input';

describe('<Input />', () => {
  test('renders component name (input)', () => {
    render(<Input />);
    const title = screen.getByText(/input/i);
    expect(title).toBeInTheDocument();
  });
});*/
export {};
